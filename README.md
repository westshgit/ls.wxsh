# WEST AWS traefik configuration

This repository contains the configuration files for the WEST AWS Traefik setup. It includes the necessary files to deploy and manage Traefik in an AWS environment.

## New Image ?

Edit the `docker-compose.yml` file to include the new image you want to use for Traefik. Make sure to specify the correct label and tag for the image.
